Bulk DNS Record creation for ACSPanel

Usage
-----

Before execute you have to copy config.py.dist to config.py and twek yout settings.

Then:

```
$ pip install -r requeriments.txt

$ python bulkdns.py
```
